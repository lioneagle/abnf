package regexpr

/*
import (
	"bytes"
	//"os"
	//"strconv"
	"testing"
	"trace"
)

func TestRegExprCat(t *testing.T) {
	testdata := []struct {
		expr1 RegExpr
		expr2 RegExpr
		str   string
	}{
		{&RegExprCat{}, &RegExprCat{}, ""},
	}
	prefix := trace.CallerName(0)

	for i, v := range testdata {

		if v.r.Size() != v.size {
			t.Errorf("%s[%d] failed: size = %s, wanted = %s\n", prefix, i, v.r.Size(), v.size)
		}
	}
}
*/
